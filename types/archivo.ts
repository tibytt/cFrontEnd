let mistrig : any= "hola"; //any tipo puede ser string

mistrig= "hola si";

let miNumero: number =5;

miNumero = 10/2;

let miBooleano: boolean =true;

function suma(a: number, b:number):number{
    return a+b;
}
//array de numeros
function agregar(arr:Array<number>, element:number): void{
arr.push(element);

}
suma(1,2);

let miArray: number[] = [1,2,3];
//en general le da tipo para q no le pongas otras cosas